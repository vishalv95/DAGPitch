View(prices)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(get.hist.quote(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
colnames(price) <- comp
prices <- cbind(prices,price)
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(get.hist.quote(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
names(price) <- comp
prices <- cbind(prices,price)
}
View(prices)
colnames(mtcars)[-1]
colnames(mtcars)
colnames(mtcars)[:-1]
colnames(mtcars)[length(colnames(mtcars))]
colnames(mtcars)[length(colnames(mtcars))] <- 'duh'
colnames(mtcars)[length(colnames(mtcars))]
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(get.hist.quote(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
View(prices)
help(cor)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
get.hist.quote
myghq <- tseries::get.hist.quote
fix(myghq)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(myghq(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
fix(myghq)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(myghq(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
getwd()
getwd()
setwd("C:/Users/Vishal/Documents/GitHub/DAG Pitch")
setwd("C:/Users/Vishal/Documents/GitHub/DAG-Pitch")
qq
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
setwd("C:/Users/Vishal/Documents/GitHub/DAG-Pitch/regressions/")
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(myghq(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(tseries)
function (package, help, pos = 2, lib.loc = NULL, character.only = FALSE,
logical.return = FALSE, warn.conflicts = TRUE, quietly = FALSE,
verbose = getOption("verbose"))
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
colnames(price) <- comp
price <- data.frame(get.hist.quote(comp, quote='Adj', start='2014-04-03'))
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
max.len <- 252
}
price <- data.frame(get.hist.quote(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- data.frame(get.hist.quote(comp, quote='Adj', start='2014-04-03'))
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
max.len <- 252
rep <- max.len - length(price[,comp])
#   price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
companies
help(get.hist.quote)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03', retclass='zoo')
price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03', retclass='zoo')
#Load Security data
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
#Load Security data
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list-filtered.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
companies <- read.table('sp500-symbol-list-filtered.txt')
companies <- read.table('sp500-symbol-list-filtered.txt')
getwd()
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list-filtered.txt')
colnames(companies) <- c('symbol')
companies <- read.table('sp500-symbol-list -filtered.txt')
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list -filtered.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
for (comp in companies$symbol){
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
rep <- max.len - length(price[,comp])
max.len <- 252
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list -filtered.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
fix(get.hist.quote)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
fix(get.his.quote)
fix(get.hist.quote)
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
install.packages(tseries)
install.packages('tseries')
install.packages("tseries")
install.packages("tseries")
install.packages("tseries")
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
install.packages('zoo')
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
library(tseries)
install.packages('tseries')
install.packages("tseries")
library(tseries)
library(MASS)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- get.hist.quote(comp, quote='Adj', start='2014-04-03')
#   price <- na.locf(price)
price <- data.frame(price)
colnames(price) <- comp
#What to do is the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
myghq<- tseries::get.hist.quote
fix(myghq)
zoo
help(zoo)
fix(myghq)
library(tseries)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- myghq(comp, quote='Adj', start='2014-04-03')
price <- data.frame(price)
colnames(price) <- comp
#What to do if the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
fix(myghq)
library(tseries)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- myghq(comp, quote='Adj', start='2014-04-03')
price <- data.frame(price)
colnames(price) <- comp
#What to do if the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
help(get.hist.quote)
library(tseries)
#Load Security data
companies <- read.table('sp500-symbol-list.txt')
colnames(companies) <- c('symbol')
prices <- matrix(0, 252, 0)
for (comp in companies$symbol){
price <- myghq(comp, quote='Adj', start='2014-04-03',retclass='its')
price <- data.frame(price)
colnames(price) <- comp
#What to do if the stock price has no data here?
max.len <- 252
rep <- max.len - length(price[,comp])
price <- c(price[,comp], rep(NA, if(rep > 0) rep else 0))
prices <- cbind(prices,price)
colnames(prices)[length(colnames(prices))] <- comp
}
help.search(zoo)
help.search('zoo')
install.packages('zoo')
